// Loop example in C+
let count = 1;
while (count <= 10) {
    if (count == 5) {
        print("Middle point reached!");
    } else {
        print("Count: " + count);
    };
    count = count + 1;
};
